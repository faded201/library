# CosyVoice TTS Requirements
# Create conda environment: conda create -n marco python=3.8
# Install: pip install -r requirements.txt

# Core TTS
torch>=1.13.0
torchaudio>=0.13.0

# Web server
flask>=2.0.0
flask-cors>=3.0.10

# Audio processing
librosa>=0.9.2
soundfile>=0.10.3

# Utilities
numpy>=1.21.0
scipy>=1.7.0

# Optional: GPU support (uncomment one)
# torch-cuda (install via conda: conda install pytorch::pytorch pytorch::pytorch-cuda=11.8 -c pytorch -c nvidia)
